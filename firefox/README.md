# Firefox configs

Saves user.js and chrome.

This is separate from the stow directories, because Firefox profile paths will
differ on each machine, and there's a lot of different state tracking in the
`.mozilla/firefox` directory. As such, we save only the configs.
